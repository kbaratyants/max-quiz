# MAX Quiz - MVP

Чат-бот/мини-приложение для мессенджера MAX, которое позволяет создавать опросы, проходить их и просматривать статистику. Упрощенная модель без привязки к аккаунтам и университетам.

## 🛠 Технологический стек

- **Backend**: Node.js + TypeScript + NestJS + MongoDB (Mongoose) + Swagger
- **Frontend**: React + TypeScript + Vite + React Router + Recharts
- **База данных**: MongoDB
- **Контейнеризация**: Docker + Docker Compose

## 📁 Структура проекта

```
hackatone/
├── backend/          # NestJS API
│   ├── src/
│   │   ├── surveys/  # Модуль опросов
│   │   ├── responses/ # Модуль ответов
│   │   └── schemas/  # MongoDB схемы
│   └── Dockerfile
├── frontend/         # React приложение
│   ├── src/
│   │   ├── pages/    # Страницы приложения (Home, CreateSurvey, TakeSurvey, MyResponses, SurveyStats)
│   │   ├── api/      # API клиент
│   │   └── utils/    # Утилиты (управление ID)
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose установлены на вашей системе

### Запуск проекта

1. Клонируйте репозиторий (если нужно) и перейдите в директорию проекта:
```bash
cd hackatone
```

2. Запустите все сервисы одной командой:
```bash
docker-compose up --build
```

Эта команда:
- Соберёт образы для backend и frontend
- Запустит MongoDB, backend и frontend
- Настроит сеть между контейнерами

3. Дождитесь запуска всех сервисов (обычно 1-2 минуты)

## 📍 Доступ к сервисам

После запуска сервисы будут доступны по следующим адресам:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3000
- **Swagger документация**: http://localhost:3000/docs
- **MongoDB**: localhost:27017

## 🔐 Идентификация

В MVP используется упрощенная модель идентификации без регистрации:

- **clientId**: автоматически генерируется при первом заходе и сохраняется в localStorage. Используется для отслеживания пройденных опросов.
- **userId**: используется для идентификации создателя опроса (по умолчанию "teacher-1", можно изменить в localStorage).

Все ID передаются через заголовки HTTP:
- `x-client-id` - ID клиента (для ответов)
- `x-user-id` - ID пользователя (для создания опросов и просмотра статистики)

## 📚 API Endpoints

### Опросы

- `POST /surveys` - Создать опрос
  - Body: `{ title: string, questions: [{ text: string, options: string[] }] }`
  - Headers: `x-user-id` (опционально, по умолчанию "teacher-1")

- `GET /surveys/mine` - Получить список созданных мной опросов
  - Headers: `x-user-id` (обязательно)

- `GET /surveys/:id` - Получить опрос по ID (для прохождения)

- `POST /surveys/:id/responses` - Отправить ответы на опрос
  - Body: `{ answers: number[] }`
  - Headers: `x-client-id` (опционально, сгенерируется автоматически)
  - Возвращает `clientId` в ответе (если был сгенерирован)

- `GET /surveys/:id/stats` - Получить статистику по опросу (только для создателя)
  - Headers: `x-user-id` (обязательно)
  - Возвращает: общее количество ответов и статистику по каждому вопросу

### Ответы

- `GET /responses/mine` - Получить список пройденных мной опросов
  - Headers: `x-client-id` (обязательно)
  - Возвращает: список с названием опроса и датой прохождения

Полная документация доступна в Swagger: http://localhost:3000/docs

## 🎯 Функциональность

Приложение предоставляет 4 основных сценария использования:

### 1. Создать опрос
- Форма для создания опроса с названием
- Динамическое добавление вопросов
- Для каждого вопроса - несколько вариантов ответов
- Сохранение опроса в базу данных

### 2. Посмотреть свои пройденные опросы
- Список всех опросов, которые прошел текущий пользователь
- Отображается название опроса и дата прохождения
- Используется `clientId` для идентификации

### 3. Пройти опрос по ID
- Ввод ID опроса (можно передать по ссылке)
- Загрузка и отображение опроса
- Выбор ответов на вопросы
- Сохранение ответов с автоматической генерацией `clientId` при необходимости

### 4. Посмотреть статистику по созданным опросам
- Список всех опросов, созданных текущим пользователем
- Детальная статистика по каждому опросу:
  - Общее количество ответов
  - По каждому вопросу: количество выборов каждого варианта ответа
  - Визуализация в виде диаграмм
- Доступ только для создателя опроса

## 🗄 Модели данных

### Survey
```typescript
{
  _id: ObjectId,
  title: string,
  ownerId: string,  // строка-идентификатор создателя
  questions: [
    {
      text: string,
      options: string[]
    }
  ],
  createdAt: Date
}
```

### Response
```typescript
{
  _id: ObjectId,
  surveyId: ObjectId,
  answers: number[],  // индексы выбранных вариантов
  clientId: string,   // строка-идентификатор прошедшего опрос
  createdAt: Date
}
```

**Важно**: Модель User удалена. Используется упрощенная идентификация через строковые ID (`ownerId` и `clientId`).

## 🏗 Архитектура

### Backend (NestJS)

- **Модульная архитектура**: разделение на модули (surveys, responses)
- **MongoDB через Mongoose**: схемы в `src/schemas/`
- **Swagger**: автоматическая документация API
- **CORS**: настроен для работы с frontend на порту 5173
- **Validation**: использование class-validator для валидации DTO
- **UUID**: генерация уникальных идентификаторов для клиентов

### Frontend (React + Vite)

- **React Router**: маршрутизация между 4 основными страницами
- **Axios**: HTTP клиент с автоматической подстановкой заголовков (x-user-id, x-client-id)
- **localStorage**: хранение clientId и userId
- **Recharts**: визуализация статистики в виде диаграмм
- **UUID**: генерация уникальных идентификаторов

## 🐳 Docker

Проект полностью контейнеризован:

- **mongo**: MongoDB 7 с персистентным томом
- **backend**: NestJS приложение на порту 3000
- **frontend**: React приложение на порту 5173

Все сервисы находятся в одной Docker сети и могут общаться по именам сервисов.

## 🔧 Разработка

### Локальная разработка (без Docker)

Если вы хотите запускать сервисы локально:

**Backend:**
```bash
cd backend
npm install
npm run start:dev
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

**MongoDB:**
Убедитесь, что MongoDB запущен локально на порту 27017.

### Переменные окружения

**Backend:**
- `PORT` - порт сервера (по умолчанию 3000)
- `MONGO_URI` - URI подключения к MongoDB

**Frontend:**
- `VITE_API_URL` - URL backend API (по умолчанию http://localhost:3000)

## 📝 Примечания

- Это MVP версия, готовая к расширению
- Интеграция с реальным MAX API не реализована - используется упрощенная модель идентификации
- Код структурирован и готов к дальнейшей разработке
- Все 4 основных сценария работают согласно схеме
- Идентификация происходит через заголовки HTTP без реальной регистрации

## 🎨 Интерфейс

Приложение состоит из 4 основных экранов:

1. **Главная страница** (`/`) - выбор действия (4 кнопки)
2. **Создать опрос** (`/create`) - форма создания опроса
3. **Пройти опрос по ID** (`/take`) - ввод ID и прохождение опроса
4. **Мои пройденные опросы** (`/my-responses`) - список пройденных опросов
5. **Статистика по созданным** (`/stats`) - список созданных опросов и их статистика

## 🐛 Известные ограничения

- Нет реальной авторизации через MAX API
- Нет пагинации для списков
- Нет обработки ошибок на уровне middleware
- clientId и userId хранятся только в localStorage браузера

## 📄 Лицензия

MIT

