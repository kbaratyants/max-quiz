# MAX Quiz

Мини-приложение для мессенджера MAX, которое позволяет создавать опросы, проходить их и просматривать статистику

## 🛠 Технологический стек

- **Backend**: Node.js + TypeScript + NestJS + MongoDB (Mongoose) + Swagger
- **Frontend**: React + TypeScript + Vite + React Router + Recharts
- **База данных**: MongoDB
- **Контейнеризация**: Docker + Docker Compose

## 📁 Структура проекта

```
/max-quiz/
│
├── backend/...
│
├── frontend/...
│
├── docker-compose.yml
│
├── requirements.txt
│
├── README.md 
│
└── presentation.pdf   

```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose установлены на вашей системе

### Запуск проекта

1. Клонируйте репозиторий (если нужно) и перейдите в директорию проекта:
```bash
cd max-quiz
```
2. Перейдите в директорию бекэнда:
```bash
cd backend
```
3. Создайте файл .env и заполните как в примере:
```
MONGO_URI=mongodb://mongo:27017/maxquiz
BASE_URL=http://localhost:3000
NODE_ENV=development   # Режим разработки
MAX_DEV_TOKEN=... # ваш токен
PORT=3000
```
Пояснения:
**BASE_URL=http://localhost:3000** - URL, где разрвернуто ваше приложение
**NODE_ENV=development** — локальная разработка с ограниченным функционалом: создание квизов, прохождение квизов по ID, просмотр данных. Полный функционал, интеграции с MAX и использование камеры в mini-app недоступны.
**NODE_ENV=production** — полный функционал: проверка данных пользователя через MAX, использование камеры внутри MAX mini-app, возможность делиться ссылками внутри мессенджера и за его пределами.
**MAX_DEV_TOKEN** необходим для интеграции с MAX.
4. Вернитесь в директорию проекта:
```bash
cd ..
```
5. В файле docker-compose.yml необходимо задать переменную VITE_API_URL для сервиса frontend:
Для локальной разработки:
environment:
  VITE_API_URL: http://localhost:3000
Для продакшена:
environment:
  VITE_API_URL: https://your-app-domain.com
6. Запустите все сервисы одной командой:
```bash
docker-compose up --build
```

Эта команда:
- Соберёт образы для backend и frontend
- Запустит MongoDB, backend и frontend
- Настроит сеть между контейнерами

7. Дождитесь запуска всех сервисов (обычно 1-2 минуты)


## 🎯 Функциональность

Приложение предоставляет 4 основных сценария использования:

### 1. Создать опрос
- Форма для создания опроса с названием
- Динамическое добавление вопросов
- Для каждого вопроса - несколько вариантов ответов
- Сохранение опроса в базу данных
- Поделиться опросом - по id, по ссылке или qr-коду

### 2. Пройти опрос
- Ввод ID опроса (можно передать по ссылке или отсканировать qr-код)
- Загрузка и отображение опроса
- Выбор ответов на вопросы
- Сохранение ответов

### 3. Посмотреть свои результаты
- Список всех опросов, которые прошел текущий пользователь
- Отображается название опроса и дата прохождения
- Возможность детально посмотреть ответы

### 4. Посмотреть статистику по созданным опросам
- Список всех опросов, созданных текущим пользователем
- Детальная статистика по каждому опросу:
  - Общее количество ответов
  - По каждому вопросу: количество выборов каждого варианта ответа
  - Визуализация в виде диаграмм
  - Детализация по пользователям
- Доступ только для создателя опроса

### Переменные окружения

**Backend:**
- `PORT` - порт сервера (по умолчанию 3000)
- `MONGO_URI` - URI подключения к MongoDB

**Frontend:**
- `VITE_API_URL` - URL backend API (по умолчанию http://localhost:3000)

## 📝 Примечания

- Это MVP версия, готовая к расширению
- Код структурирован и готов к дальнейшей разработке
- Все 4 основных сценария работают согласно схеме

## 📄 Лицензия

MIT

