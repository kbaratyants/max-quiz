# MAX Quiz - MVP

Чат-бот/мини-приложение для мессенджера MAX, которое позволяет преподавателям создавать опросы/квизы, а студентам проходить их.

## 🛠 Технологический стек

- **Backend**: Node.js + TypeScript + NestJS + MongoDB (Mongoose) + Swagger
- **Frontend**: React + TypeScript + Vite + React Router + Recharts
- **База данных**: MongoDB
- **Контейнеризация**: Docker + Docker Compose

## 📁 Структура проекта

```
hackatone/
├── backend/          # NestJS API
│   ├── src/
│   │   ├── auth/     # Модуль авторизации
│   │   ├── surveys/  # Модуль опросов
│   │   └── schemas/  # MongoDB схемы
│   └── Dockerfile
├── frontend/         # React приложение
│   ├── src/
│   │   ├── pages/    # Страницы приложения
│   │   ├── api/      # API клиент
│   │   └── context/  # React контексты
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose установлены на вашей системе

### Запуск проекта

1. Клонируйте репозиторий (если нужно) и перейдите в директорию проекта:
```bash
cd hackatone
```

2. Запустите все сервисы одной командой:
```bash
docker-compose up --build
```

Эта команда:
- Соберёт образы для backend и frontend
- Запустит MongoDB, backend и frontend
- Настроит сеть между контейнерами

3. Дождитесь запуска всех сервисов (обычно 1-2 минуты)

## 📍 Доступ к сервисам

После запуска сервисы будут доступны по следующим адресам:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3000
- **Swagger документация**: http://localhost:3000/docs
- **MongoDB**: localhost:27017

## 🔐 Авторизация

В MVP используется мок-авторизация для тестирования. При первом запуске приложения вы увидите форму входа.

### Примеры для тестирования:

**Преподаватель:**
- maxId: `teacher1`
- Роль: `teacher`

**Студент:**
- maxId: `student1`
- Роль: `student`

Вы можете использовать любые другие значения maxId - система автоматически создаст пользователя при первом входе.

## 📚 API Endpoints

### Авторизация

- `POST /auth/mock` - Мок-авторизация
  ```json
  {
    "maxId": "teacher1",
    "role": "teacher"
  }
  ```

### Опросы

- `POST /surveys` - Создать опрос (требуется заголовок `x-user-id`)
- `GET /surveys?mine=true` - Получить список опросов (только свои, если `mine=true`)
- `GET /surveys/:id` - Получить опрос по ID
- `POST /surveys/:id/responses` - Отправить ответы на опрос
- `GET /surveys/:id/stats` - Получить статистику по опросу

Полная документация доступна в Swagger: http://localhost:3000/docs

## 🎯 Функциональность

### Для преподавателя:

1. **Создание опросов**
   - Создание квизов (с правильными ответами) или опросов обратной связи
   - Добавление вопросов с несколькими вариантами ответов
   - Указание правильных ответов для квизов

2. **Просмотр статистики**
   - Общее количество ответов
   - Средний балл (для квизов)
   - Детальная статистика по каждому вопросу с диаграммами

### Для студента:

1. **Прохождение опросов**
   - Просмотр доступных опросов
   - Прохождение опросов с выбором ответов
   - Получение результата (для квизов)

## 🗄 Модели данных

### User
```typescript
{
  _id: ObjectId,
  maxId: string,
  role: "teacher" | "student"
}
```

### Survey
```typescript
{
  _id: ObjectId,
  title: string,
  type: "quiz" | "feedback",
  ownerId: ObjectId,
  questions: [
    {
      text: string,
      options: string[],
      correctOptionIndex?: number
    }
  ],
  createdAt: Date
}
```

### Response
```typescript
{
  _id: ObjectId,
  surveyId: ObjectId,
  userId?: ObjectId,
  answers: number[],
  score?: number,
  createdAt: Date
}
```

## 🏗 Архитектура

### Backend (NestJS)

- **Модульная архитектура**: разделение на модули (auth, surveys)
- **MongoDB через Mongoose**: схемы в `src/schemas/`
- **Swagger**: автоматическая документация API
- **CORS**: настроен для работы с frontend на порту 5173
- **Validation**: использование class-validator для валидации DTO

### Frontend (React + Vite)

- **React Router**: маршрутизация между страницами
- **Context API**: управление состоянием авторизации
- **Axios**: HTTP клиент с автоматической подстановкой заголовков
- **Recharts**: визуализация статистики

## 🐳 Docker

Проект полностью контейнеризован:

- **mongo**: MongoDB 7 с персистентным томом
- **backend**: NestJS приложение на порту 3000
- **frontend**: React приложение на порту 5173

Все сервисы находятся в одной Docker сети и могут общаться по именам сервисов.

## 🔧 Разработка

### Локальная разработка (без Docker)

Если вы хотите запускать сервисы локально:

**Backend:**
```bash
cd backend
npm install
npm run start:dev
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

**MongoDB:**
Убедитесь, что MongoDB запущен локально на порту 27017.

### Переменные окружения

**Backend:**
- `PORT` - порт сервера (по умолчанию 3000)
- `MONGO_URI` - URI подключения к MongoDB

**Frontend:**
- `VITE_API_URL` - URL backend API (по умолчанию http://localhost:3000)

## 📝 Примечания

- Это MVP версия, готовая к расширению
- Интеграция с реальным MAX API не реализована (используется мок-авторизация)
- Код структурирован и готов к дальнейшей разработке
- Все основные CRUD операции работают

## 🐛 Известные ограничения

- Нет реальной авторизации через MAX API
- Нет проверки прав доступа (любой пользователь может создавать опросы)
- Нет пагинации для списков
- Нет обработки ошибок на уровне middleware

## 📄 Лицензия

MIT

